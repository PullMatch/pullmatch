{
  "name": "@pullmatch/shared",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Shared types and utilities for PullMatch",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test src/__tests__/*.test.ts"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
